version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd walking-library
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  base-directory: walking-library/dist
  files:
    - '**/*'
